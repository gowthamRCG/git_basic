# git_basic
**Git Basics:** Git is a version control system used to track, save, and manage changes in your project. It keeps a history of your work, while GitHub is an online platform used to store, share, and collaborate on Git repositories. Common commands include `git init`, `git status`, `git add`, `git commit`, `git push`, `git pull`, and `git clone`.
